-- =====================================================================
-- V020: reservation (reservations / seat_states)
-- Owner: Person C
-- See docs/data_model.md §3.
-- =====================================================================

-- TODO(C): reservations / seat_states を作成。CHECK制約を忘れずに (HoldSeats が頼る不変条件)。
SELECT 1;
