-- =====================================================================
-- V001: shared kernel + identity
-- Owner: Person A
-- See docs/data_model.md §1 for the authoritative spec.
-- =====================================================================

-- TODO(A): users / domain_events_outbox を docs/data_model.md §1 通りに作成する。
-- Migration が空のままだと Flyway は記録だけ作ってOKなので、現時点ではプレースホルダ。
SELECT 1;
